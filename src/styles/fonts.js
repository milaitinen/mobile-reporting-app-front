import { StyleSheet } from "react-native";

export const fonts = StyleSheet.create({
  normal: { fontSize: 15 },
  alternate: { fontSize: 25, color: "#FFFFFF" },
  big: { fontSize: 20, alignSelf: "center" }
});

export const scalingFactors = { normal: 20, big: 10 };
